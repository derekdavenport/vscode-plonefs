{
	"name": "vscode-plonefs",
	"displayName": "PloneFS",
	"description": "Open Plone sites",
	"version": "0.1.2",
	"publisher": "derekdavenport",
	"repository": {
		"url": "https://github.com/derekdavenport/plonefs.git",
		"type": "git"
	},
	"engines": {
		"vscode": "^1.29.0"
	},
	"categories": [
		"Other"
	],
	"activationEvents": [
		"onFileSystem:plone",
		"onCommand:plonefs.workspace"
	],
	"main": "./out/src/extension",
	"contributes": {
		"commands": [
			{
				"command": "plonefs.workspace",
				"title": "Open Plone Workspace",
				"category": "PloneFS"
			},
			{
				"command": "plonefs.setTitle",
				"title": "Set Title",
				"category": "PloneFS"
			},
			{
				"command": "plonefs.setDescription",
				"title": "Set Description",
				"category": "PloneFS"
			}
		],
		"menus": {
			"explorer/context": [
				{
					"when": "resourceScheme == plone",
					"command": "plonefs.setTitle",
					"group": "PloneFS"
				},
				{
					"when": "resourceScheme == plone",
					"command": "plonefs.setDescription",
					"group": "PloneFS"
				}
			]
		}
	},
	"scripts": {
		"vscode:prepublish": "npm run compile",
		"vscode:package": "vsce package",
		"vscode:publish": "vsce publish",
		"compile": "tsc -p ./",
		"watch": "tsc -watch -p ./",
		"postinstall": "node ./node_modules/vscode/bin/install",
		"test": "jest",
		"test:coverage": "jest --coverage",
		"test:integration": "npm run compile && node ./node_modules/vscode/bin/test"
	},
	"devDependencies": {
		"@types/node": "^10.12.10",
		"@types/jest": "^23.3.9",
		"typescript": "^3.1.6",
		"vscode": "^1.1.22",
		"tslint": "^5.8.0"
	},
	"dependencies": {
		"mime": "^2.3.1"
	}
}